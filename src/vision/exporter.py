"""Exporter: convert .pt model to .tflite (placeholder).
Implement conversion logic depending on your toolchain (torch->onnx->tflite)"""

def convert_pt_to_tflite(pt_path: str, out_path: str):
    # placeholder
    print(f"Converting {pt_path} -> {out_path}")
